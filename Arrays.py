# ----------- ARRAY DATA STRUCTURE -------
""" 
Arrays are data structu res used when working with large amount of data.
it is efficient and consumes less  memory.
its is gotten by imposrting and arrary functions
"""

from array import array

# How to create an array functions

numbers = [1, 2, 76, 80]
numbers = array(('i'),[1, 2, 76, 80])
print(numbers)

numbers.append(50)
print(numbers)